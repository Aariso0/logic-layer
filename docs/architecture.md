# Architecture decision records (ADRs) for Logic Layer.
# One ADR per significant design choice: format, verdict semantics, KB structure,
# agent-agnostic middleware contract, etc. Use adr-NNNN-short-title.md naming.